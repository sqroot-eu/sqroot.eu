# Site settings
title: SQroot.eu
header-img: content/site/home-bg.jpg
email: ando@sqroot.eu
description: "A personal engineering blog"
baseurl: ""
url: "https://sqroot.eu"
timezone: Europe/Tallinn

# Build settings
markdown: kramdown
highlighter: rouge
permalink: "/:year/:title"
paginate: 15
exclude: ["Gemfile", ".idea", "Gemfile.lock", "nginx.conf", "Dockerfile", "package.json", "LICENSE","README.md",".travis.yml", "vendor", "node_modules"]
include: [".well-known"]

plugins:
  - jekyll-paginate

assets:
  sources:
    - _assets/js
    - _assets/css
    - _assets/lib
    - content
    - node_modules
  js_compressor: uglifier
  css_compressor: sass

compress_html:
  ignore:
    envs:
      - development
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Default settings
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts" # previously `post` in Jekyll 2.2.
    values:
      author: "Ando"
      layout: "post"
  -
    scope:
      path: ""
      type: "pages"
    values:
      visible: true
