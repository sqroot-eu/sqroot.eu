language: ruby
rvm: 
  - 2.1.2
install:
  - gem install jekyll html-proofer
script:
  - jekyll build
#  - htmlproof _site
archive: true
after_script:
  - mv _site shippable

branches:
  only:
    - master
    - develop
cache: true
